spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  seed:
    enabled: true
  migration:
    user-role:
      enabled: true

logging:
  level:
    vn.edu.hcmuaf.fit.fashionstore: DEBUG
    org.hibernate.SQL: DEBUG
